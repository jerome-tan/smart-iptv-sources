{
  "name": "smart-iptv-sources",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "python3 generate.py",
    "generate:check-streams": "python3 generate.py --check-streams",
    "generate:node": "node scripts/generate.mjs",
    "validate": "python3 validate.py",
    "validate:node": "node scripts/validate.mjs",
    "test": "python3 validate.py",
    "refresh": "python3 scripts/refresh.py",
    "refresh:dry": "python3 scripts/refresh.py --dry-run"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
